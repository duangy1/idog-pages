import Button from '@material-ui/core/Button';
function Header() {
  return (
    <div>
      <div>hi im header</div>
      <Button variant="contained" color="primary">
        你好，世界
        </Button>
    </div>
  );
}

export default Header;

